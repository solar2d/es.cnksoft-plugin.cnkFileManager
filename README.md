Sources for the plugin `plugin.cnkFileManager`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.cnkFileManager" = {
            publisherId = "es.cnksoft",
        },
    },
}
```
